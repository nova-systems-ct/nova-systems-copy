# Nova Product Build — Requirement Matrix

Generated 2026-09-23 against commit `69fef38`; updated same day through commit `9af43eb`
(login-path verification, vault link-lifetime correction, CRM/Order/Audit foundations, Zion
Studio). Tracks the 24-section "NOVA — PRODUCT-ONLY MASTER EXECUTION PROMPT"
against verified repository/live-database/live-production evidence — not intent, not what a
previous prompt described, not what a route's mere existence implies.

**Status vocabulary** (per the master prompt's own definitions): `not started` · `in development`
· `locally verified` · `staging verified` · `waiting on owner/provider` · `ready for
installation` · `installed` · `live verified`.

**How to read this table**: "Evidence" cites the actual file, migration, or test run that backs
the status — not a description of intended behavior. Where a prior session's report turned out to
be wrong (the VITE_SUPABASE_URL login theory), that correction is noted explicitly rather than
silently dropped.

| ID | Source | Module | Depends on | Current evidence | Acceptance test | Status | Blocker |
|----|--------|--------|-----------|-------------------|------------------|--------|---------|
| R01 | §4 | Login path — system-level correctness | Supabase Auth, canonical RBAC | Live-verified 2026-09-23: production URL/CSP/anon key all correct (re-checked); `scripts/e2e_login_path_test.mjs` 12/12 against live DB (wrong-password rejection, real session issuance/persistence/sign-out, no-membership = zero access, revoked membership = zero access, active membership = full access — all via real RPC calls as the actual user, not service-role); real Playwright run against `https://nova-systems.app/login` with a throwaway account reached `/dashboard`, survived a refresh, rendered real content, zero console errors, and an unauthenticated direct hit to `/dashboard/admin` correctly redirected to `/login` | T01 (partial — no real owner account tested) | **live verified** (system level) | none — this module works |
| R02 | §4 | Login path — Isaac's specific account | R01 | `isaac@nova-systems.app`: confirmed, not banned, real historical successful sign-in on file. A password-reset request for it is REJECTED by Supabase itself (`email_address_invalid`) despite `nova-systems.app` having a valid MX record (ruled out as a DNS/deliverability issue). `isaac_0427@icloud.com`: reset email dispatched successfully (200); **directly queried `organization_members` for this account 2026-09-23 and confirmed a real, pre-existing (created in the original Stage 4 bootstrap, not created now) active `nova_super_admin` row on the real Nova Systems org** — a working reset path was explicitly not treated as sufficient proof; the actual membership record was checked | Isaac completes a real reset/login using `isaac_0427@icloud.com`, or investigates the `email_address_invalid` rejection in Supabase Dashboard → Authentication → Email/URL Configuration | **waiting on owner** (membership itself is confirmed correct — nothing left to fix in code) | Requires Isaac's own Supabase Dashboard access — no Management API token exists in this environment to inspect or fix Auth email-validation settings |
| R03 | §3 | Canonical identity/tenancy/RBAC foundation | — | `organizations`/`organization_members`/`permissions`/`role_permissions` live in production since Stage 4 (2026-09-13); `scripts/e2e_org_isolation_test.mjs` 8/8 and `scripts/e2e_api_client_auth_test.mjs` 22/22, both re-run 2026-09-23, both still passing | T02 | **live verified** | none |
| R04 | §7 | Six-hub dashboard shell (Overview/Intelligence/Growth/Execution/Companies/Admin) | R03 | Shipped Stage 3-4.1; `src/components/dashboard/DashboardLayout.jsx`, real permission-filtered nav, org switcher | — | **live verified** (shell only — module *contents* vary, see below) | Owner/company split view (§7's "Run Nova / Run Companies") NOT built — current shell is single-context, not yet split |
| R05 | §7/§12 | Growth hub — Leads + shared CRM (businesses, contacts, deals) | R03, migration | `src/pages/dashboard/Leads.jsx` (pre-existing, real). **New 2026-09-23**: `businesses`/`crm_contacts`/`crm_deals` (full pipeline + stage_history) in `supabase/crm-order-audit-migration-standalone.sql`, `api/client.js`'s `crm` resource (real object-level per-org authorization, not just permission-string checks), `src/pages/dashboard/CRM.jsx`. `scripts/validate_crm_order_audit_schema.mjs` 8/8 (pg-mem); `scripts/e2e_crm_order_audit_test.mjs` written, fails fast on missing schema | T02, T06 (partial) | **locally verified** (code), **waiting on owner** (schema) | Migration not applied. Outreach sequencing, calendar engine, workflow builder, CSV import/export still not built (see R13) |
| R06 | §7 | Growth hub — Referrals, Wave One (legacy), Blog, Portfolio, Newsletter | R03 | Pre-existing pages, real Supabase-backed as of the 2026-09-21/23 fake-data repair pass (`Newsletter.jsx` real as of commit `2abbf5e`) | — | **live verified** | "Wave One" here is the legacy application-tracking page, NOT §10's communications/CRM bundle. **2026-09-23**: reconciled at the data-model level — the new bundle is seeded into the `products` catalog as `communications-crm-bundle` v1, a separately named/versioned entry, explicitly not renaming/merging this page. Its actual runtime (missed-call handling, inbound routing, unified inbox) is R11, still not built |
| R07 | §6/§15 | Nova Sales Academy (10 programs, quiz engine, certificates) | R03, migration | Fully coded: `api/academy.js`, `src/pages/dashboard/Academy*.jsx`, schema in `supabase/academy-migration-standalone.sql`. `scripts/e2e_academy_auth_test.mjs` written (13 checks) — fails fast on missing schema, confirmed unchanged 2026-09-23 | T08 (partial) | **locally verified** (code), **waiting on owner** (schema) | Migration not applied — no DDL access from this environment. 80% pass threshold remains a proposed default pending Isaac's formal approval — does not block other work |
| R08 | §15 | Hiring pipeline (application → invite → candidate → Academy → practical review → agreement → activation) | R03, R07, migration | Fully coded: `api/intake.js` (invite-applicant/my-application/resume-signed-url/activate-representative), `api/contracts.js` (application_id linkage), `nova_sales_candidate` role. `scripts/e2e_hiring_workflow_test.mjs` written (13 checks) — fails fast on missing schema, confirmed unchanged 2026-09-23 | T08 | **locally verified** (code), **waiting on owner** (schema) | Same migration file as R07, plus the storage-bucket blocker (R09) for private uploads specifically |
| R09 | §3/§15 | Private file storage (resumes, contracts, invoices, generated docs, portfolio images) | — | **Real defect found 2026-09-23**: none of the three buckets this codebase references (`portfolios`, `portfolio`, `nova-vault`) exist in production — confirmed live via Storage Admin API (404 on all three). Every upload path in the app has been silently failing. Code rewritten to the correct target state (private path storage, on-demand signed URLs). **Corrected same day per explicit instruction**: the first fix stored a 30-day signed link, reasoned as necessary for emailed invoices/contracts — re-checked and that reasoning was wrong (both already attach the PDF directly, never a link to this bucket); `signVaultUrl`'s default dropped to 5 minutes, `uploadToVault` no longer persists any URL at all (only `storage_path`), and `NovaVault.jsx` now calls the existing `resign` action fresh on every view/download instead of trusting a stored link | T09 | **waiting on owner** (buckets) | Bucket creation attempt was blocked by this session's own permission classifier ("Modify Shared Resources"); exact spec in the build-state doc |
| R10 | §8/§9 | Nova Audit / diagnostic engine (evidence, findings, report builder, 24-72h clock, delivery) | R03, migration | **Built 2026-09-23, real domain model**: `audit_cases`/`audit_evidence`/`audit_findings`/`audit_recommendations`/`audit_reports`/`audit_deliveries`/`audit_outcomes` in `supabase/crm-order-audit-migration-standalone.sql`; `api/client.js`'s `audit` resource; a genuinely pure, independently unit-tested deadline-clock module (`api/_auditClock.js`, `scripts/unit_audit_clock_test.mjs` 13/13, zero database dependency — covers business-hours weekend-skip and the pause-extends-by-exactly-its-duration invariant); report-version immutability enforced (re-approving/editing an approved version is refused, verified live in `scripts/e2e_crm_order_audit_test.mjs`); **2026-09-24, approval authorization/currency evidence added per explicit request**: a real stale-version gap was found and fixed — nothing previously stopped approving an OLD draft after a newer one was created; now refused (409). `scripts/e2e_crm_order_audit_test.mjs`'s section 7 explicitly proves all three required cases in sequence: an unauthorized cross-org staff member cannot approve (403), the same authorized caller cannot approve a stale v1 after v2 exists (409), and the same caller approving the actual current version (v2) succeeds (200); `src/pages/dashboard/AuditCases.jsx` + `AuditCaseDetail.jsx` (case queue with live clock status, evidence/findings/recommendations/report tabs, pause/resume controls) | T03 | **locally verified** (code + pure-function unit tests), **waiting on owner** (schema) | Migration not applied. Branded PDF/report-content renderer not built yet — versioned immutable report *records* exist, the rendered document itself is the next increment (flagged in the Report tab's own UI, not hidden) |
| R11 | §10 | Wave One (real communications-to-CRM bundle as specified in §10) | R03, R12 (voice) | `api/waves-intake.js`/`WaveOne.jsx` exist but implement a much narrower thing: an application/enrollment tracker for a past "Wave One" product launch, not missed-call handling, inbound routing, or a unified inbox. **2026-09-23**: reconciled at the catalog level (see R06) — a real `communications-crm-bundle` product row now exists, correctly separately named/versioned, `readiness_state = 'not started'`. The actual runtime is unbuilt | T05 | **not started** (runtime); catalog entry **live verified** pending migration | Needs R12 (voice/phone infrastructure) as a real dependency before missed-call handling can work at all |
| R12 | §11 | 24/7 AI voice/call agents | R03 | Zero implementation. No telephony provider credentials configured in `.env.local` (checked: no Twilio voice-specific webhook handler, no persistent-connection runtime) | T05 | **not started** | Needs a hosted, persistent-connection-capable runtime — current Vercel serverless functions cannot host live call audio; provider/account decisions needed first |
| R13 | §12 | CRM pipelines, outreach, calendar engine, workflow builder | R03, migration | Deal pipeline + stage_history now real (see R05). Outreach sequencing, calendar/booking engine, and the workflow-template runtime are NOT built | T06/T07 | **in development** (deal pipeline only) | Migration not applied for the pipeline piece; calendar/workflow/outreach need their own schema pass |
| R14 | §13 | Installation Center (product catalog, order states, client provisioning) | R03, migration | **Product catalog and order lifecycle real** (`products`, `orders`/`order_events`, full 17-state machine, unapproved pricing never invented). **Provisioning workflow built 2026-09-23**: `installations`/`installation_tests`/`installation_events` (`supabase/installation-center-migration-standalone.sql`, pg-mem validated 6/6), `api/client.js`'s `installations` resource — sandbox_setup → sandbox_testing (real recorded test runs) → sandbox_passed (re-derived from the LATEST recorded test being an actual pass, never a separately trusted flag) → active, gated by `admin.view` specifically ("activation authority," a distinct higher-trust gate from the `growth.view` every other installation action uses) → pause/resume (reason required) / offboard (terminal, reason required). One installation per order enforced by a real UNIQUE constraint (409 on a duplicate attempt). `src/pages/dashboard/Installations.jsx` + `InstallationDetail.jsx`, both gated `admin.view` matching every other Admin-hub page. `scripts/e2e_installation_center_test.mjs` written — includes the exact T13 acceptance test (two independent test orgs, proving Org B can neither read nor write Org A's installation, and Org B's own list never leaks Org A's row) — fails fast on missing schema | T13 | **locally verified** (code) | Migration not applied (also depends on the CRM/Audit migration for `orders`). Activation is currently a manual per-installation action — no automated sandbox-to-production promotion pipeline, which the spec does not require |
| R15 | §16 | Zion Studio (journal → character video pipeline) | R03, migration | **Built 2026-09-23**: `zion_journal_entries`/`zion_facts`/`zion_character_assets`/`zion_video_ideas`/`zion_scripts`/`zion_storyboards`/`zion_videos`/`zion_review_events` in `supabase/zion-studio-migration-standalone.sql`; `api/client.js`'s `zion` resource; `src/pages/dashboard/ZionStudio.jsx` (Journal/Ideas & Scripts/Review tabs). A real pre-ship authorization gap (ideas/scripts/videos/review only checked "any staff," not growth.view) was found and fixed before this shipped — covered by `scripts/e2e_zion_studio_test.mjs`. Render step correctly refuses (409, honest message) with no character asset on file; no render provider connected, and the code says so rather than faking a result | T11 | **locally verified** (code) | Migration not applied. No character reference assets supplied yet (blocks the render step only, per instruction — not the rest of the Studio, which is fully built and tested). Calendar/Published/Analytics/Revenue/Profile Bible tabs from §16 not built |
| R16 | §17 | Marketing/content/SEO engine | R03, migration | **Real review/scheduling workflow built 2026-09-24** on top of the existing, already-live blog_posts table (title/slug/category/excerpt/content/SEO fields already real): `status` (draft→in_review→approved→scheduled→published), `scheduled_at`, `submitted_by/at`, `approved_by/at`, `review_notes` added (`supabase/marketing-content-workflow-migration-standalone.sql`, pg-mem validated 5/5, including a proof the one-time backfill UPDATE is idempotent and doesn't touch already-draft rows). **Closed a real, live authorization gap**: any `growth.view` staff member could previously flip `published` straight to `true` with zero review — `handleBlogAdmin`'s `save` action no longer accepts `published`/`status` from the client at all; going live is only reachable via `submit-for-review` → `approve` (requires `admin.view` specifically, re-checked on top of the dispatcher's blanket `growth.view`, same pattern as Installation Center activation) → `publish-now`/`schedule`. In-review posts aggregate into the Approval Inbox as `item_type: 'content'`. **Also fixed a real bug in last night's Approval Inbox**: its `audit_report` aggregation never included `organization_id` in the `approve_action` body, which `handleAuditReports` requires on every POST — every "Approve" click on an audit report from the inbox would have failed; fixed via a PostgREST embed (`audit_cases(organization_id)`) through the existing `case_id` FK. `Blog.jsx` rebuilt around the new status states (no more direct publish toggle). `scheduled_at` records intent only — no durable worker exists yet to act on it automatically (see R19); publishing is always an explicit real action. `scripts/e2e_marketing_content_workflow_test.mjs` written, fails fast on missing schema. Broader multi-channel marketing automation (social scheduling, email campaign sequencing beyond the existing Newsletter primitive) is still not started — this closes the specific "no workflow/approval/scheduling" gap named in this matrix, not the full §17 scope | T12 (partial) | **locally verified** (code) | Migration not applied. Social/email campaign automation beyond blog content review remains not started |
| R17 | §14 | Crystal / Company 002 operational workspace | R03 | Zero implementation. Naming still provisional per historical context | T10 | **not started** | Blocked behind R10-R13 per this prompt's own dependency order (§23 step 8 comes after CRM/Audit/Wave One) |
| R18a | §19 | Integration Center | R03 | **Correction to this matrix's own earlier entry**: this is NOT zero implementation — checked before assuming otherwise, per the master prompt's explicit "do not rebuild... preserve and extend" instruction. `api/integrations.js` + `src/pages/dashboard/Integrations.jsx` are a real, complete registry for all 8 providers this codebase touches (Supabase, Stripe, Resend, Anthropic, Twilio, Google, ElevenLabs, Deepgram) — honest status vocabulary matching §19 exactly (`not_connected`/`disconnected`/`connected`/`tested`/`degraded`/`authorization_required`), on-demand live testing (never automatic), env-var presence checking with zero secret exposure, already routed and in the Admin hub nav | — | **live verified** | Presence/live-test only, no persistent history table — "last successful sync"/"webhook health"/"usage" from §19's full field list aren't tracked across time, only computed fresh on each check. No per-organization OAuth-style connections yet (all 8 current providers are Nova's own global credentials, not per-client integrations) |
| R18b | §19 | Approval Inbox | R03, migration | **Built 2026-09-23**: `approval_requests` table (`supabase/approval-inbox-migration-standalone.sql`, pg-mem validated 3/3), `api/client.js`'s `approvals` resource (GET aggregates pending audit reports + Zion videos + generic requests into one real inbox; POST create/approve/reject/revoke) gated at `admin.view`, `src/pages/dashboard/ApprovalInbox.jsx` (real list/action UI, wired into the Admin hub + nav). Deliberately does not retrofit `audit_reports`/`zion_videos`' own already-tested approval logic — those keep their existing flows; the inbox's approve/reject buttons for those two item types call straight through to the SAME endpoints already covered by `e2e_crm_order_audit_test.mjs`/`e2e_zion_studio_test.mjs`. Content-version mismatch is refused (409), expiry auto-marks the row `expired` on a late approve attempt (410), revoke is only valid from an actually-`approved` state (a real logic bug — revoke guarded by the same "must be pending" check as approve/reject, making it permanently unreachable — was caught and fixed before this shipped). `scripts/e2e_approval_inbox_test.mjs` written (aggregation, version mismatch, expiry, revoke-from-approved, admin.view gating), fails fast on missing schema | T14 | **locally verified** (code), **waiting on owner** (schema) | Migration not applied. Vercel function count still 12/12 — folded entirely into `api/client.js`, no new top-level file |
| R19 | §20 | Durable workers/queues, cost tracking, incident monitoring | R03, migration, hosting decision | **Real runtime built and recommended 2026-09-24**: `docs/RUNTIME_HOSTING_RECOMMENDATION.md` gives a concrete, sourced recommendation (Fly.io, ~$2–8/mo, one small always-on worker) rather than an open question. `jobs` table + `claim_next_job()` SQL function (`supabase/durable-jobs-migration-standalone.sql`, pg-mem validated 4/4); `worker/jobQueueEngine.mjs` pure claim/complete/fail/retry/dead-letter algorithm, `scripts/unit_job_queue_test.mjs` **12/12 — includes a real proof of crash recovery** (a job whose worker "dies" mid-processing is automatically reclaimed once its lease expires, with zero operator intervention), exponential backoff, and dead-letter after exhausted retries. `worker/index.mjs` is a genuinely runnable poll loop, verified today with a real local end-to-end run (`node worker/index.mjs --local --enqueue-demo --once`) — real persistence to disk, confirmed no duplicate processing on a second run. Wired into a real consumer: `handleBlogAdmin`'s `schedule` action now enqueues a real `marketing_publish` job (not just an unread `scheduled_at` field), proven in `scripts/e2e_marketing_content_workflow_test.mjs`. Cost tracking/incident monitoring beyond this are still not built | T15 | **locally verified** (queue engine + worker), **waiting on owner** (schema + Fly.io account/hosting decision) | Migration not applied. Worker not deployed anywhere yet — runs locally only until Isaac authorizes a Fly.io account per the runtime doc's owner-action list. Cost-per-org/provider tracking and incident escalation from §20 not yet built on top of this foundation |
| R20 | §9 | Vercel function capacity | — | Re-verified 2026-09-23: exactly 12 of 12 top-level `api/*.js` files. Confirmed still the actual constraint (not assumed) — this is the real current plan limit, not an unverified carryover number | — | **live verified** | Any new top-level endpoint requires consolidating into an existing file or confirming a plan upgrade with Isaac |

## What this table intentionally does NOT do

It does not restate every one of the master prompt's ~200 individual acceptance criteria as a
separate row — that would produce a document nobody reads rather than one that's actually used to
track real work. Each row above represents one coherent module; the "Current evidence" column
points at the actual code/test/live-check backing the status claim, so any row can be re-verified
independently rather than taken on faith.

## Summary counts (updated 2026-09-24, commit pending — post-Durable-Jobs/Runtime-Recommendation)

- **Live verified**: R01 (system-level login), R03, R04 (shell), R06 (catalog reconciliation), R18a (Integration Center — found already real, corrected from an earlier wrong "not started" entry in this same matrix), R20 — 6
- **Locally verified, waiting on owner for schema/infra**: R05, R07, R08, R10, R14, R15, R16 (partial — see row), R18b, R19 — 9
- **In development (partial, real, not yet complete)**: R11 (catalog done, runtime not), R13 (pipeline only) — 2
- **Waiting on owner (no code blocker)**: R02, R09 — 2
- **Not started**: R12, R17 — 2

Twenty-one rows now cover the original twenty modules (R18 split into R18a/R18b once it turned
out to be two different things at two different states). This matrix will be updated, not
replaced, as each module moves — see `docs/PRODUCT_BUILD_STATE.md` for the live checkpoint and
exact next task. **Lesson applied from this correction**: verify a module's real state by reading
its actual code before writing "not started" in this table, the same discipline already used for
the Academy/hiring/CRM domains — this one slipped through on a first pass and was caught before it
caused any wasted rebuild effort.
